var config = {
    paths: {
        'bootstrap': 'js/bootstrap.min',
        'popper': 'js/popper.min'
    },
    shim: {
        'bootstrap': {
            'deps': ['jquery', 'popper']
        }
    }
};
